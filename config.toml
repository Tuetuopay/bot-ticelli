[auth]
token = "youmadbro"

[db-config]
database-url = "postgres://bot:bot@localhost:5432/bot"

[bot-config]
command-prefix = "!"
win-sentences = [
    "Bravo {}, plus un dans votre pot à moutarde. A vous la main.",
    "GG {}, un point pour Gryffondor !",
]
ratelimit = { delay = 5, time-span = 30, limit = 4 }
